/* Optional integration points, compatible with the previous site.
   No endpoint or key was configured in the source V4.
   Email preparation is the working default; never simulate a successful send.
   Set LATITUDE_LEAD_SUBMITTER (a function returning a Promise), or a real
   LATITUDE_ODOO_LEAD_ENDPOINT, or LATITUDE_WEB3FORMS_KEY before contact.js.
*/
window.LATITUDE_SITE = { languageStorage: 'latitude-v5-language', version: 5 };
