/* Latitude Samui — production configuration. No credentials belong in this file. */
window.LATITUDE_SITE = {version: 8};
// Optional HTTPS endpoint accepting the lead JSON from js/contact.js.
// Leave unset to use the working email enquiry flow.
// window.LATITUDE_ODOO_LEAD_ENDPOINT = 'https://your-domain.example/api/leads';
